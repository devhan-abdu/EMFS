#!/bin/sh
set -eu

echo "🚀 Running pre-push checks..."

export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
if [ -s "$NVM_DIR/nvm.sh" ]; then
  set +u; . "$NVM_DIR/nvm.sh"; [ -f .nvmrc ] && nvm use >/dev/null; set -u
fi

echo "⚡ Fast incremental typechecking..."
pnpm typecheck

echo "✅ Typecheck passed!"